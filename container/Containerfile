# File generated by source to image (s2i v1.3.6) executable, by running
#     ./s2i build exporters registry.access.redhat.com/ubi8/python-39 \
#     IMAGE:TAG --loglevel 2 --as-dockerfile Containerfile
# in the project ROOT and removing labels and COPY source.
# To build image, in the project ROOT, run
#     docker image build --tag IMAGE:TAG -f container/Containerfile .
#     docker container run --rm -e APP_FILE=EXPORTER_TYPE/app.py IMAGE:TAG

FROM registry.access.redhat.com/ubi8/python-39

USER root
# Installs skopeo for committime exporter
RUN dnf -y install skopeo
# Copying in source code
COPY ../exporters /tmp/src
# Change file ownership to the assemble user. Builder image must support chown command.
RUN chown -R 1001:0 /tmp/src
USER 1001
# Assemble script sourced from builder image based on user input or image metadata.
# If this file does not exist in the image, the build will fail.
RUN /usr/libexec/s2i/assemble
# Run script sourced from builder image based on user input or image metadata.
# If this file does not exist in the image, the build will fail.
CMD /usr/libexec/s2i/run
